# 図書館保守システム — 機能仕様書

## 1. ドキュメント概要

| 項目 | 内容 |
|------|------|
| ドキュメント名 | 機能仕様書 |
| ファイル名 | `04_機能仕様書.md` |
| アプリ名 | 図書館保守システム（libraly_app） |
| 目的 | 認証・貸出返却・各マスタ CRUD の振る舞いを定義する |
| 実装 | HTML 6 画面 + `js/auth.js` + `css/auth-bar.css` |
| 関連ドキュメント | `01_要求仕様書.md` / `02_DB仕様書.md` / `03_画面仕様書.md` |

### 1.1 画面と機能の対応

| ファイル | 主な機能 ID |
|----------|-------------|
| `index.html` | F-AUTH-01〜02（UI）、F-LEND-01〜08 |
| `admin.html` | F-AUTH-04〜05 のみ（ポータル） |
| `books.html` | F-01〜07 |
| `categories.html` | F-CAT-01〜04 |
| `students.html` | F-STU-01〜05 |
| `masters.html` | F-FAC-01〜04、F-DEP-01〜04 |
| `js/auth.js` | F-AUTH-03〜05（共通） |

---

## 2. 開発前提（機能関連）

| 項目 | 方針 |
|------|------|
| 言語 | HTML / CSS / JavaScript のみ |
| Supabase SDK | CDN（`@supabase/supabase-js` v2） |
| クライアント | `LibralyAuth.getClient()`（`js/auth.js`）で全画面共有 |
| 認証 | Supabase Auth（メール＋パスワード）。UI は **`index.html` のみ** |
| RLS | 使用しない |
| user_id | 業務テーブルには持たない |
| エラー（認証） | `alert()` |
| エラー（業務） | 各画面の `#message` または `alert()`（返却確認は `confirm()`） |

**用語の対応**

| 呼び方 | 意味 | DB |
|--------|------|-----|
| 図書 | 1件の登録データ | `libraly_app` |
| 図書カテゴリ | 図書に任意で付与する分類 | `category_id` → `libraly_app_category` |
| 学生 | 貸出者 | `libraly_app_student` |
| 貸し出し履歴 | 図書と学生の貸出・返却記録 | `libraly_app_lending_history` |

---

## 3. 機能一覧

| 機能ID | 機能名 | 画面 | Supabase 操作 |
|--------|--------|------|---------------|
| F-AUTH-01 | 新規登録 | `index.html` | `auth.signUp` |
| F-AUTH-02 | ログイン | `index.html` | `auth.signInWithPassword` |
| F-AUTH-03 | ログアウト | 全画面（認証バー） | `auth.signOut` |
| F-AUTH-04 | ログイン状態表示 | 全画面 | `auth.getSession` |
| F-AUTH-05 | 未ログインガード | 全画面 | `auth.getSession` → 未ログイン時 `index.html` へ |
| F-LEND-01 | 貸出登録 | `index.html` | `insert` on `libraly_app_lending_history` |
| F-LEND-02 | 履歴一覧表示 | `index.html` | `select`（図書・学生情報を結合またはキャッシュ参照） |
| F-LEND-03 | 返却 | `index.html` | `update`（`is_returned: true`） |
| F-LEND-04 | 貸出中図書の選択制限 | `index.html` | クライアント（履歴キャッシュ） |
| F-LEND-05 | 履歴状態絞り込み | `index.html` | クライアント |
| F-LEND-06 | 履歴キーワード検索 | `index.html` | クライアント |
| F-LEND-07 | 貸出用マスタ読込 | `index.html` | `select` on 図書・学生・カテゴリ・学部・学科 |
| F-01 | 図書新規保存 | `books.html` | `insert` |
| F-02 | 図書一覧表示 | `books.html` | `select`（カテゴリ結合） |
| F-03 | 図書編集 | `books.html` | `update` |
| F-04 | 図書削除 | `books.html` | `delete` |
| F-05 | メッセージ表示 | 各画面 | — |
| F-06 | カテゴリ選択肢取得 | `books.html` | `select` on `libraly_app_category` |
| F-07 | 一覧カテゴリ絞り込み | `books.html` | クライアントフィルタ |
| F-CAT-01〜04 | カテゴリマスタ CRUD | `categories.html` | `insert` / `select` / `update` / `delete` |
| F-STU-01〜05 | 学生マスタ CRUD・検索 | `students.html` | CRUD + 学部学科マスタ参照 |
| F-FAC-01〜04 | 学部マスタ CRUD | `masters.html` | CRUD on `libraly_app_faculty` |
| F-DEP-01〜04 | 学科マスタ CRUD | `masters.html` | CRUD on `libraly_app_department` |

---

## 4. 共通仕様

### 4.1 Supabase クライアント

- `js/auth.js` の `LibralyAuth.getClient()` で 1 インスタンスを共有
- URL・anon key は `js/auth.js` に定数（本書には記載しない）

### 4.2 認証フロー

| 画面 | 起動時 |
|------|--------|
| `index.html` | `getSession()` → なし: `#auth-gate` / あり: `#app-main` + 業務データ読込 |
| その他 | `requireAuth()` → 失敗時 `index.html` へ `replace` → `renderUserBar('#auth-bar')` |

ログイン・新規登録成功時は **別 URL へ遷移せず** `index.html` 上で `#app-main` を表示する。

### 4.3 通信中

- 主要ボタンを `disabled` にし、二重送信を抑止（`setBusy` 等）

---

## 5. 認証機能（F-AUTH-01〜05）

### 5.1 新規登録（F-AUTH-01）

| 項目 | 内容 |
|------|------|
| UI | `index.html` の「新規登録」タブ（`#signup-form`） |
| API | `auth.signUp({ email, password })` |
| 成功 | `showAppMain()` — 貸出・返却 UI 表示・マスタ読込 |
| 失敗 | `alert(LibralyAuth.authErrorMessage(error))` |

### 5.2 ログイン（F-AUTH-02）

| 項目 | 内容 |
|------|------|
| UI | `index.html` の「ログイン」タブ（`#login-form`） |
| API | `auth.signInWithPassword({ email, password })` |
| 成功 | F-AUTH-01 と同様 |
| 失敗 | `alert()` |

### 5.3 ログアウト（F-AUTH-03）

| 項目 | 内容 |
|------|------|
| トリガー | `#auth-bar` の「ログアウト」 |
| API | `auth.signOut()` |
| 成功 | `location.replace('index.html')` — 未ログイン表示 |

### 5.4 ログイン状態表示（F-AUTH-04）

- `session.user.email` を「ログイン中:」の右に表示
- 対象: `index.html`（`#app-main` 内）および全保守画面

### 5.5 未ログインガード（F-AUTH-05）

- `admin.html` / `books.html` / `categories.html` / `students.html` / `masters.html` で必須
- セッションなし → `index.html` へリダイレクト

### 5.6 バリデーション（認証）

| チェック | 条件 | 失敗時 |
|----------|------|--------|
| メール | `trim()` 後、長さ > 0 | `alert` |
| パスワード | 長さ ≥ 6 | `alert`（パスワードは6文字以上で入力してください。） |

---

## 6. 貸出・返却機能（F-LEND-01〜07）

**実装:** `index.html`（`#app-main` 内）

### 6.1 貸出登録（F-LEND-01）

| 順番 | 処理 |
|------|------|
| 1 | 図書・学生をドロップダウンで選択 |
| 2 | 「貸出する」押下 |
| 3 | `libraly_app_lending_history` に `insert`（`is_returned: false`） |
| 4 | 履歴・図書選択肢を再描画 |

### 6.2 貸出中図書の再貸出防止（F-LEND-04）

- 履歴キャッシュから `is_returned === false` の `book_id` を収集
- 図書ドロップダウンで該当図書を `disabled`

### 6.3 返却（F-LEND-03）

- 貸出中の行のみ「返却」ボタン
- `confirm()` で確認後、`is_returned: true` に `update`

### 6.4 履歴表示・絞り込み（F-LEND-02, F-LEND-05, F-LEND-06）

| 機能 | 内容 |
|------|------|
| 一覧 | 状態・図書名・著者・学籍番号・氏名・貸出日時・返却状態 |
| 状態絞り込み | すべて / 貸出中のみ / 返却済みのみ（初期: 貸出中のみ） |
| キーワード | 図書名・著者・氏名の部分一致（条件は AND） |

### 6.5 貸出フォームの絞り込み（F-LEND-07）

- 図書: カテゴリでドロップダウン絞り込み
- 学生: 学部 → 学科 → 学生の順で絞り込み

---

## 7. 図書マスタ機能（F-01〜07）

**実装:** `books.html`

### 7.1 図書新規保存（F-01）

タイトル・あらすじ・著者と、任意の図書カテゴリを新規登録する。

| 入力名 | DBカラム |
|--------|----------|
| タイトル | `title` |
| あらすじ | `content` |
| 著者 | `name` |
| 図書カテゴリ | `category_id`（未選択時 `null`） |

```javascript
.from('libraly_app').insert({
  title, content, name,
  category_id: categoryId || null
})
```

### 7.2 図書一覧表示（F-02）

- `libraly_app` を select（カテゴリ名結合）、`created_at` 降順
- 未設定カテゴリは「未設定」表示
- F-07 の絞り込み後に描画

```javascript
.from('libraly_app')
.select('id, title, content, name, category_id, created_at, libraly_app_category ( name )')
.order('created_at', { ascending: false })
```

### 7.3 図書編集（F-03）

- 「編集」でフォームに値をセット、`editingId` を保持
- 「更新」で `update`（`category_id` は null 可）

### 7.4 図書削除（F-04）

- 確認後 `delete`
- 貸出履歴がある図書は DB 制約で削除不可（エラー表示）

### 7.5 メッセージ表示（F-05）

| 種別 | 例 |
|------|-----|
| 成功 | 図書を保存しました / 更新しました / 削除しました |
| エラー | 必須入力エラー、通信失敗 |

### 7.6 カテゴリ選択肢取得（F-06）

- `libraly_app_category` から `name` 昇順
- フォーム先頭は「未選択」

### 7.7 一覧カテゴリ絞り込み（F-07）

- 見出し横の select でクライアント側フィルタ
- 「すべて」で全件

---

## 8. 図書カテゴリマスタ（F-CAT-01〜04）

**実装:** `categories.html`

| 機能ID | 概要 |
|--------|------|
| F-CAT-01 | カテゴリ名を登録（`name` UNIQUE） |
| F-CAT-02 | 一覧表示（名前昇順） |
| F-CAT-03 | カテゴリ名を編集 |
| F-CAT-04 | 削除（確認あり。図書の `category_id` は `SET NULL`） |

---

## 9. 学生マスタ（F-STU-01〜05）

**実装:** `students.html`

| 機能ID | 概要 |
|--------|------|
| F-STU-01 | 学籍番号・氏名・学科・生年月日（YYYYMMDD）を登録 |
| F-STU-02 | 一覧（学部・学科名は JOIN 表示） |
| F-STU-03 | 編集 |
| F-STU-04 | 削除（貸出履歴がある場合は不可） |
| F-STU-05 | 学部絞り込み・学籍番号・氏名検索 |

- 学部・学科は `masters.html` のマスタからドロップダウン選択

---

## 10. 学部・学科マスタ（F-FAC / F-DEP）

**実装:** `masters.html`

| 機能ID | 概要 |
|--------|------|
| F-FAC-01〜04 | 学部の CRUD |
| F-DEP-01〜04 | 学科の CRUD（所属学部必須） |

- 学部削除: 配下学科がある場合は不可（RESTRICT）
- 学科削除: 学生が参照している場合は不可

---

## 11. ページ読み込み時の実行順

### index.html

```
1. getSession()
2a. 未ログイン → #auth-gate 表示（終了）
2b. ログイン済 → showAppMain() → renderUserBar → 図書・学生・履歴等を並列 load
```

### 保守画面（例: books.html）

```
1. requireAuth()
2. renderUserBar
3. loadCategories → loadBooks
4. イベント設定
```

---

## 12. 対象外

| 分類 | 内容 |
|------|------|
| 認証 | OAuth、マジックリンク、メール確認、業務データの user_id 分離、RLS |
| その他 | 専用ログイン HTML、Next.js / React、npm ビルド |

---

## 13. 受入の目安

| 機能ID | 確認項目 |
|--------|----------|
| F-AUTH-01 | 新規登録後、同一 `index.html` で貸出画面が表示される |
| F-AUTH-02 | ログイン後、同上 |
| F-AUTH-03 | ログアウト後、`index.html` でログイン UI が表示される |
| F-AUTH-04 | 全画面でメールアドレスが表示される |
| F-AUTH-05 | 未ログインで `books.html` 等を開くと `index.html` へ飛ぶ |
| F-LEND-01 | 貸出登録が履歴に反映される |
| F-LEND-03 | 返却後、状態が返却済みになる |
| F-LEND-04 | 貸出中の図書は貸出 select で選べない |
| F-01〜04 | `books.html` で図書 CRUD ができる |
| F-06〜07 | カテゴリ選択・一覧絞り込みが動作する |

---

## 14. 改訂履歴

| 版 | 日付 | 内容 |
|----|------|------|
| 1.0 | 2026-05-25 | 初版（図書 CRUD・`index.html` 想定） |
| 1.1 | 2026-05-25 | カテゴリ（F-06）対応 |
| 1.2 | 2026-05-25 | 一覧絞り込み（F-07） |
| 1.3 | 2026-06-01 | 認証（F-AUTH）追記 |
| 1.4 | 2026-06-01 | 認証 UI を `index.html` に集約 |
| 2.0 | 2026-06-01 | 現行実装全体（貸出・各マスタ・6 画面）に合わせて全面改訂 |
